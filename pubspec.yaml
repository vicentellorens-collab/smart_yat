name: smart_yat
description: "SmartYat - Intelligent Yacht Management System"
publish_to: 'none'
version: 2.3.0+1

environment:
  sdk: ^3.5.0

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  speech_to_text: ^7.0.0
  http: ^1.2.0
  provider: ^6.1.0
  shared_preferences: ^2.2.0
  intl: ^0.19.0
  google_fonts: ^6.2.0
  permission_handler: ^11.3.0
  local_auth: ^2.3.0
  supabase_flutter: ^2.5.0
  camera: ^0.11.0
  image_picker: ^1.1.0
  connectivity_plus: ^6.0.0
  flutter_tts: ^4.0.0
  flutter_secure_storage: ^9.0.0
  file_picker: ^8.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
